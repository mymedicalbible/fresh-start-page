node_modules
dist

# Supabase CLI local state (should not be committed)
supabase/.temp/

.env
.env.local
.env.playwright
.DS_Store

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
/playwright/.auth/

# Local code export bundles (npm run export)
medical-bible-code-export-*.zip

# Cloudflare Wrangler local cache (this app is deployed as static Pages `dist`, not `wrangler deploy`)
.wrangler/

# Export script staging dirs inside exports/ (zips in exports/ are kept)
exports/_staging_*/
