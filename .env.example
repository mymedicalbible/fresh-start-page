# Copy to `.env` or `.env.local` for local dev. Use the same project as `supabase link`
# (Settings → API: Project URL = VITE_SUPABASE_URL, anon public = VITE_SUPABASE_ANON_KEY).
# Production (Cloudflare Pages, etc.) must set the same two vars for the DB you `db push` to,
# or the app will call a different database than your migrations.
VITE_SUPABASE_URL=https://YOUR_PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_key

# Database password (Settings → Database). Needed for `npm run supabase:push` so the CLI does not hang on "Initialising login role".
# SUPABASE_DB_PASSWORD=your_database_password

# Optional (local / CI only): service_role secret from Supabase → Settings → API.
# Lets `npm run test:e2e` auto-create `scripts/ensure-playwright-smoke-user.mjs` + `.env.playwright`.
# Never ship this to the browser or commit it; keep it only in `.env` / CI secrets.
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# Plushie shop (`/app/plushies`): off by default. Set to true to enable token earns/RPCs and shop routes.
# Requires plushie migrations through at least `20260411120000_game_tokens_trial` and later rotation/catalog files.
# VITE_GAME_TOKENS_ENABLED=true

# When plushies are off: URL for one Lottie JSON (defaults to `/lottie/simple-mascot.json` in `public/`).
# VITE_SIMPLE_MASCOT_LOTTIE=/lottie/my-mascot.json

# Web Push (Profile): generate keys with `npx web-push generate-vapid-keys`. Public key here and in Edge secret
# WEB_PUSH_VAPID_PUBLIC_KEY; private key only in Supabase Edge secrets (never VITE_*).
# VITE_WEB_PUSH_PUBLIC_KEY=
# Long random string; same as Edge secret PUSH_REMINDER_CRON_TOKEN for `npm run push:run` / scheduled cron.
# PUSH_REMINDER_CRON_TOKEN=
#
# GitHub Actions (`.github/workflows/push-reminders.yml`): add repo secrets VITE_SUPABASE_URL and
# PUSH_REMINDER_CRON_TOKEN so the scheduled workflow can call the function without a local .env.
