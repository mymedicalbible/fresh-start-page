Medical Bible Project — exports (everything stays in THIS folder)

  1.txt, 2.txt, 3.txt
    THREE files with your whole app source + every Supabase .sql + configs (split evenly by file count).
    Regenerate (overwrite):  npm run export:txt
    Read 1 → 2 → 3 in order. File 1 includes a "READ ORDER" guide (path spans per part) for AI/human review.

  *.zip
    From scripts (full project, plush bundle, git archive) — run the PowerShell
    scripts or npm run export

Always look HERE:
  Medical Bible Project\exports\
